{
  "name": "@continuum/zsh-collector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix ../.. run build -w @continuum/contracts && npm --prefix ../.. run build -w @continuum/core && node --test test/*.test.mjs",
    "verify": "npm test"
  }
}
