#!/bin/sh
git_dir=$(git rev-parse --absolute-git-dir 2>/dev/null) || exit 0
helper="$git_dir/continuum/collector.mjs"
[ -f "$helper" ] || exit 0
(node "$helper" post-checkout --old "$1" --new "$2" --branch-checkout "$3" </dev/null >/dev/null 2>&1) &
exit 0
